import { CustomValueDirective } from './custom-value.directive';

describe('CustomValueDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValueDirective();
    expect(directive).toBeTruthy();
  });
});
